 "use client";import {useRouter} from "next/navigation";import {useState} from "react";export default function Login(){const r=useRouter();const [role,setRole]=useState<"ADMIN"|"CASHIER">("ADMIN");return <div className="login"><form className="loginCard" onSubmit={e=>{e.preventDefault();r.push(role==="ADMIN"?"/admin/dashboard":"/cajero/pos")}}><h1>POS INVENTARIO</h1><p className="muted">Acceso por rol</p><div className="toolbar"><button type="button" className="btn" onClick={()=>setRole("ADMIN")}>Administrador</button><button type="button" className="btn" onClick={()=>setRole("CASHIER")}>Cajero</button></div><div className="field"><label>Usuario</label><input className="input" required/></div><div className="field"><label>Contraseña</label><input className="input" type="password" required/></div><button className="btn" style={{width:"100%"}}>Iniciar sesión</button><p className="muted" style={{fontSize:12}}>Demo de interfaz. La autenticación persistente requiere DATABASE_URL + AUTH_SECRET.</p></form></div>}