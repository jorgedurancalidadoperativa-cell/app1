"use client";import AppShell from "@/components/AppShell";import {useStore} from "@/lib/store";import {useState} from "react";
export default function Usuarios(){const {users,setUsers}=useStore();const [name,setName]=useState(""),[username,setUsername]=useState(""),[role,setRole]=useState<"ADMIN"|"CASHIER">("CASHIER");function add(){if(!name||!username)return;setUsers(x=>[...x,{id:crypto.randomUUID(),name,username,role,active:true}]);setName("");setUsername("")}return <AppShell><h1 className="title">Usuarios</h1><p className="subtitle">Administración de cuentas y roles</p><div className="card" style={{marginTop:18}}><div className="grid grid3"><input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Nombre"/><input className="input" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Usuario"/><select className="select" value={role} onChange={e=>setRole(e.target.value as any)}><option value="CASHIER">Cajero</option><option value="ADMIN">Administrador</option></select></div><button className="btn btnPrimary" style={{marginTop:12}} onClick={add}>Agregar usuario</button></div><div className="tableWrap" style={{marginTop:18}}><table><thead><tr><th>Usuario</th><th>Nombre</th><th>Rol</th><th>Estado</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.username}</td><td>{u.name}</td><td>{u.role==="ADMIN"?"Administrador":"Cajero"}</td><td><span className="badge ok">Activo</span></td><td><button className="btn btnGhost" onClick={()=>setUsers(xs=>xs.map(x=>x.id===u.id?{...x,active:!x.active}:x))}>{u.active?"Desactivar":"Activar"}</button></td></tr>)}</tbody></table></div></AppShell>}
