"use client";import AppShell from "@/components/AppShell";import {useStore} from "@/lib/store";import {useState} from "react";
export default function Proveedores(){const {products}=useStore();const [q,setQ]=useState("");const names=[...new Set(products.map(p=>p.supplier).filter(Boolean))].filter(x=>x.toLowerCase().includes(q.toLowerCase()));return <AppShell><h1 className="title">Proveedores</h1><p className="subtitle">Directorio de proveedores asociados a productos</p><div className="toolbar"><input className="input" style={{maxWidth:420}} value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar proveedor..."/><button className="btn btnPrimary" onClick={()=>alert("Formulario de proveedor listo para ampliarse")}>+ Nuevo proveedor</button></div><div className="grid grid3">{names.map(n=><div className="card" key={n}><h3>{n}</h3><p className="muted">{products.filter(p=>p.supplier===n).length} productos asociados</p><span className="badge ok">Activo</span></div>)}</div></AppShell>}
