"use client";import AppShell from "@/components/AppShell";import {useStore} from "@/lib/store";import {useState} from "react";
export default function Configuracion(){const {config,setConfig}=useStore();const [saved,setSaved]=useState(false);function update(k:string,v:any){setConfig(c=>({...c,[k]:v}))}return <AppShell><h1 className="title">Configuración</h1><p className="subtitle">Datos del negocio y reglas de inventario</p><div className="card" style={{marginTop:18}}><div className="grid grid2"><div className="field"><label className="label">Nombre comercial</label><input className="input" value={config.businessName} onChange={e=>update("businessName",e.target.value)}/></div><div className="field"><label className="label">RFC</label><input className="input" value={config.rfc} onChange={e=>update("rfc",e.target.value)}/></div><div className="field"><label className="label">Dirección</label><input className="input" value={config.address} onChange={e=>update("address",e.target.value)}/></div><div className="field"><label className="label">Teléfono</label><input className="input" value={config.phone} onChange={e=>update("phone",e.target.value)}/></div><div className="field"><label className="label">Correo</label><input className="input" value={config.email} onChange={e=>update("email",e.target.value)}/></div><div className="field"><label className="label">Leyenda del ticket</label><input className="input" value={config.legend} onChange={e=>update("legend",e.target.value)}/></div><div className="field"><label className="label">Stock mínimo predeterminado</label><input className="input" type="number" value={config.defaultMinimum} onChange={e=>update("defaultMinimum",Number(e.target.value))}/></div><div className="field"><label className="label">Impuesto (%)</label><input className="input" type="number" value={config.taxRate} onChange={e=>update("taxRate",Number(e.target.value))}/></div></div><label style={{display:"flex",gap:10,marginTop:16}}><input type="checkbox" checked={config.taxEnabled} onChange={e=>update("taxEnabled",e.target.checked)}/> Activar impuestos</label><label style={{display:"flex",gap:10,marginTop:10}}><input type="checkbox" checked={config.allowNegative} onChange={e=>update("allowNegative",e.target.checked)}/> Permitir inventario negativo</label><div className="toolbar"><button className="btn btnPrimary" onClick={()=>setSaved(true)}>Guardar configuración</button>{saved&&<span className="badge ok">Configuración guardada</span>}</div></div></AppShell>}
