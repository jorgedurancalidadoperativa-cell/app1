 "use client";
import {useRouter} from "next/navigation"; import {useState} from "react";
export default function Login(){const r=useRouter();const [role,setRole]=useState<"ADMIN"|"CASHIER">("ADMIN");const [u,setU]=useState("");const [p,setP]=useState("");return <div className="login"><form className="loginCard" onSubmit={e=>{e.preventDefault();r.push(role==="ADMIN"?"/admin/dashboard":"/cajero/pos")}}><h1>POS INVENTARIO</h1><p className="subtitle">Acceso seguro por rol</p><div className="toolbar"><button type="button" className={`btn ${role==="ADMIN"?"btnPrimary":""}`} onClick={()=>setRole("ADMIN")}>Administrador</button><button type="button" className={`btn ${role==="CASHIER"?"btnPrimary":""}`} onClick={()=>setRole("CASHIER")}>Cajero</button></div><div className="grid" style={{marginTop:16}}><div className="field"><label className="label">Usuario</label><input className="input" required value={u} onChange={e=>setU(e.target.value)}/></div><div className="field"><label className="label">Contraseña</label><input className="input" required type="password" value={p} onChange={e=>setP(e.target.value)}/></div><button className="btn btnPrimary btnWide" type="submit">Iniciar sesión</button></div><div className="hint">Demo: cualquier usuario/contraseña. En producción se valida en servidor.</div></form></div>}