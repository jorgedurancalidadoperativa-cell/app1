 "use client";
import AppShell from "@/components/AppShell"; import {useState} from "react";
export default function Entradas(){const [msg,setMsg]=useState("");return <AppShell><h1 className="title">Entradas de Mercancía</h1><p className="subtitle">Registrar compras y aumentar existencias automáticamente</p><div className="card" style={{marginTop:18}}><div className="grid grid2"><div className="field"><label className="label">Proveedor</label><input className="input" placeholder="Proveedor"/></div><div className="field"><label className="label">Factura / referencia</label><input className="input" placeholder="FAC-000123"/></div><div className="field"><label className="label">Código de barras</label><input className="input"/></div><div className="field"><label className="label">Cantidad recibida</label><input className="input" type="number" min="1"/></div><div className="field"><label className="label">Precio de compra</label><input className="input" type="number" step="0.01"/></div><div className="field"><label className="label">Observaciones</label><input className="input"/></div></div><div className="toolbar"><button className="btn btnPrimary" onClick={()=>setMsg("Entrada registrada correctamente. El stock se incrementará al confirmar con base de datos.")}>Confirmar entrada</button>{msg&&<span className="badge ok">{msg}</span>}</div></div></AppShell>}
